<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gma.gmagame.mapper.LikesMapper">
    <insert id="plusLike">
        insert into LIKES_BAS(LIKENO , BNO , USER_ID)
        values((SELECT NVL(MAX(likeno), 0) + 1 FROM LIKES_BAS) ,#{bno} ,#{userId})
    </insert>

    <select id="updateBoardLike">
        UPDATE BOARD_AAA
           SET
        LIKES = (SELECT COUNT(*)
        FROM LIKES_BAS
        WHERE BNO = #{board_idx})
        WHERE BOARD_IDX = #{board_idx}
    </select>

    <select id="likeCheck" resultType="int">
        select count(*) from LIKES_BAS where BNO = #{bno} and USER_ID = #{userId}
    </select>
</mapper>